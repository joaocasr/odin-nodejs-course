<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Champions League Inventory</title>
  <style>

    body { margin: 0; font-family: Arial, sans-serif; background-color: #2519ae87; }

    nav {
      background-color: #1c2760; 
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.8rem 2rem;
    }
    nav .logo { font-size: 1rem; font-weight: bold; }
    .logoimg{
      display: block;
    }
    .ucllogo{
      margin: 10px;
    }
    nav ul { list-style: none; display: flex; gap: 1rem; margin: 0; padding: 0; }
    nav ul li a { color: white; text-decoration: none; font-weight: 500; }
    nav ul li a:hover { text-decoration: underline; }

    table {
        width: 90%;
        border-collapse: collapse;
        background: #24305f;
        border-radius: 12px;
        overflow: hidden;
        margin-top:50px;
        margin-left:100px;
        margin-right:20px;
        margin-bottom: 200px;
    }

    thead {
        background: #0b2670;
        color:rgb(100, 100, 100);
    }

    th, td {
        padding: 12px;
        text-align: left;
        color:rgb(201, 199, 199);
    }

    tbody tr:nth-child(even) {
        background: #233675;
    }

    tbody tr:hover {
        background: #2c4baa;
        cursor: pointer;
    }
    
    footer {
      background-color: #04104d;
      color: white;
      text-align: center;
      padding: 2rem 1rem;
      margin-top: 2rem;
      background-image: url('/logos/logofooter.png');
      position: fixed;
      left: 0px;
      bottom: 0px;
      width: 100%;
      clear: both;
      position: relative;
    }
    .season{
        color: rgb(12, 49, 73);
        font-size: 45px;
        display: flex;
        justify-content: center;
    }
    
    footer a { color: #f5c518; text-decoration: none; margin: 0 0.5rem; }
    footer a:hover { text-decoration: underline; }

    .placards-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
        gap: 25px;
        margin-bottom: 200px;
    }

    .placard {
        color:white;
        background: #2d365a;
        border-radius: 14px;
        padding: 20px;
        box-shadow: 0 10px 25px rgba(0,0,0,0.4);
        transition: transform 0.2s ease;
    }

    .placard:hover {
        transform: translateY(-5px);
    }

    .placard-header {
        display: flex;
        justify-content: space-between;
        font-size: 0.9rem;
        opacity: 0.8;
        margin-bottom: 15px;
    }

    .teams {
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .team {
        color:white;
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 40%;
    }

    .team-name {
        color:white;
        font-weight: bold;
        text-align: center;
        margin-bottom: 5px;
    }

    .score {
        font-size: 2rem;
        font-weight: bold;
    }

    .vs {
        color:white;
        font-size: 1rem;
        opacity: 0.7;
    }
    .teamlogo{
        width:35px;
        margin:4px;
    }
    .updatebtn{
        background-color: #297ae4;
        border-radius: 4px;
        text-align: center;
        border: solid 1px white;
        width: 80px;
        padding: 10px;
        box-shadow: 5px 5px #04104d;
    }
    .updatebtn:hover{
      cursor:pointer
    }
    .deletebtn{
        background-color: #b42424;
        border-radius: 4px;
        width: 80px;
        text-align: center;
        border: solid 1px white;
        padding: 10px;
        box-shadow: 5px 5px rgb(59, 15, 15);
    }
    .deletebtn:hover{
      cursor:pointer
    }
    .navlink{
        background-color: #1e3a8a;
        border-radius: 4px;
        border: solid 1px white;
        padding: 10px;
    }
    .searchdiv{
      display: inline-flex;
      justify-content: center;
      align-content: center;
      width: 50%;
    }
    .searchbtn{
      background-color: #1c2760;
      color:white;
      width:100px;
      height: 40px;
      box-shadow: 5px 5px #0a133f;
      border-radius: 10px;
      border: solid 1px #0a133f;
    }
    .addplayerbtn{
      background-color: #a0abe2;
      color:rgb(0, 0, 0);
      width:240px;
      font-weight: 800;
      font-size: 16px;
      height: 40px;
      box-shadow: 5px 5px #0a133f;
      border-radius: 10px;
      border: solid 1px #0a133f;
    }
    .addplayerbtn:hover{
      cursor:pointer;
    }
    .operations-section{
      display: inline-flex;
      width: 100%;
      justify-content: center;
    }
    .adddiv{
      display: inline-flex;
    }
    .searchbox{
      background-color: #727eb6;
      color:white;
      width:560px;
      height: 30px;
      padding:10px;
      border-radius: 10px;
      border: solid 1px #3e4668;
    }
    .searchbtn:hover{
      cursor: pointer;
    }
  </style>
</head>
<body>

  <!-- --- Navbar --- -->
  <nav>
    <div class="logoimg">
    <div class="logo">Champions League Inventory</div>
    <div>
      <img class="ucllogo" width="80px" height="80px" src="/logos/logo.png"/>
    </div>
    </div>
    <ul>
      <div class="navlink"><li><a href="/">Home</a></li></div>
      <div class="navlink"><li><a href="/teams">Teams</a></li></div>
      <div class="navlink"><li><a href="/players/seasons/<%= season %>/<%= seasonid %>">Players</a></li></div>
    </ul>
  </nav>

    <main>
        <h3 class="season">Season <%= season %> Players</h3> 
        <% if(search==true){ %>
        <h5 class="season">Results for players with name containing: <%= name %></h5> 
        <%} %>
        <div class="operations-section">
          <div class="searchdiv">
            <form action="/search/players/<%= season %>/<%= seasonid %>" method="GET">
              <input class="searchbox" type="text" id="name" name="name" value="" placeholder="Search for player name...">
              <input class="searchbtn" type="submit" value="search">
            </form>
          </div>
          <div class="adddiv">
            <form action="" method="GET">
              <input class="addplayerbtn" type="submit" value="+ Add Player">
            </form>
          </div>
        </div>
        
     <table id="playersTable">

      <thead>
        <tr>
          <th>Name</th>
          <th>Real Name</th>
          <th>Country</th>
          <th>Position</th>
          <th></th>
          <th></th>
        </tr>
      </thead>

      <tbody>

        <% players.forEach(player => { %>
          <tr>
            <td onclick="location.href='/players/<%= seasonid %>/<%= player.player_id %>'"><%= player.name %></td>
            <td onclick="location.href='/players/<%= seasonid %>/<%= player.player_id %>'"><%= player.real_name || "-" %></td>
            <td onclick="location.href='/players/<%= seasonid %>/<%= player.player_id %>'"><%= player.country %></td>
            <td onclick="location.href='/players/<%= seasonid %>/<%= player.player_id %>'"><%= player.position %></td>
            <td><button class="updatebtn">Update</button></td>
            <td><form action="/delete/players/<%= season %>/<%= seasonid %>/<%= player.player_id %>" method="POST"><button type="submit" onclick="return confirm('Are your sure you want to delete <%= player.name %>?')" class="deletebtn">Delete</button></td>
          </tr>
        <% }) %>

      </tbody>

    </table>
    </main>

  <!-- --- Footer --- -->
  <footer>
    <p>&copy; 2026 Champions League Inventory. All Rights Reserved.</p>
    <p>
      <a href="/about">About</a> |
      <a href="/contact">Contact</a> |
      <a href="/privacy">Privacy Policy</a>
    </p>
  </footer>

</body>
</html>